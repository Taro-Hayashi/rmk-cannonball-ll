name: Build reset-bootloader firmware

on:
  workflow_dispatch:
  push:
    paths:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: thumbv7em-none-eabihf
          components: llvm-tools

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-reset-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-reset-

      - name: Install flip-link
        run: cargo install flip-link

      - name: Install cargo-binutils
        run: cargo install cargo-binutils

      - name: Install cargo-hex-to-uf2
        run: cargo install cargo-hex-to-uf2

      - name: Build reset-bootloader firmware
        run: |
          cargo objcopy --release -- -O ihex rmk-cannonball-ll-reset-bootloader.hex
          cargo hex-to-uf2 --input-path rmk-cannonball-ll-reset-bootloader.hex --output-path rmk-cannonball-ll-reset-bootloader.uf2 --family nrf52840

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: rmk-cannonball-ll-reset-bootloader
          path: |
            rmk-cannonball-ll-reset-bootloader.hex
            rmk-cannonball-ll-reset-bootloader.uf2
